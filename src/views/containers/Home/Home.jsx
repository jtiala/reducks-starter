import React from 'react';
import styles from './Home.scss';

const Home = () => (
  <section>
    <span role="img" aria-label="duck" className={styles.duck}>🦆</span>
  </section>
);

export default Home;
